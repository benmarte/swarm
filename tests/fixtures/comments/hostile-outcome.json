{
  "schema": "swarm/outcome@1",
  "role": "validator",
  "verdict": "confirmed",
  "refs": { "issue": 99 },
  "evidence": {
    "summary": "Issue with `backticks` and ${INJECTION} and $(command) and 'quotes'",
    "checks": ["safe value", "another `backtick` check", "${HOME} injection attempt"]
  }
}
