# Fixture env with a hostile secret value containing shell metacharacters.
# The bootstrap script must never interpolate or echo this value.
SWARM_GITHUB_TOKEN=xoxb-"; echo pwned
ANTHROPIC_API_KEY=$(whoami)
SWARM_TOKEN=`id -u`
